# Place the exported TensorFlow Lite model here after training:
#   ml/scripts/export_tflite.py
#
# Expected files:
#   sat_plantscan_cassava.tflite
#   labels.txt
#
# Without the .tflite file, the app uses a heuristic fallback and still runs offline.
